<div class="container">
  <div class="page-header">
    <%= link_to 'Delete', course_path(@course), method: :delete, class: 'btn btn-danger pull-right' %>
    <h1>Edit&nbsp;<%= @course.title %></h1>
  </div>
  <%= render 'form' %>
</div>
